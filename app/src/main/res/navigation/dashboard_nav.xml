<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/dashboard_nav"
    app:startDestination="@id/dailyGraph">

    <fragment
        android:id="@+id/dailyGraph"
        android:name="com.example.donation.DailyGraph"
        android:label="fragment_daily_graph"
        tools:layout="@layout/fragment_daily_graph" />
    <fragment
        android:id="@+id/monthlyGraph"
        android:name="com.example.donation.MonthlyGraph"
        android:label="fragment_monthly_graph"
        tools:layout="@layout/fragment_monthly_graph" />
</navigation>